# Copy this file to .env and fill in real values before running.
# Never commit .env to source control.

DJANGO_SECRET_KEY=replace-with-a-long-random-string
DJANGO_DEBUG=False

# Redis (matches the service name in docker-compose)
REDIS_HOST=redis
REDIS_PORT=6379
